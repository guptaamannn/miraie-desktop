# Disable pnpm's minimum release age supply-chain policy.
# This policy rejects packages published within a configurable window,
# which causes CI failures when dependencies update frequently.
min-publish-age=0
