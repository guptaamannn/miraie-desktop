[project]
name = "python-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aman Gupta", email = "guptaamannn@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "miraie-ac>=1.1.2",
    "pyinstaller>=6.22.2",
]

[project.scripts]
python-api = "python_api:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
